# {{ eventtype }} events for {{ toolname }}
# from {{ start }} to {{ end }}
Start (ISO)	End (ISO)	Minutes	Project	User	Affiliation	Title/Remarks
{% for e in events %}{{ e.start|date:"y-m-d,H:i" }}	{{ e.end|date:"y-m-d,H:i" }}	{{ e.minutes }}	{{ e.projectname }}	{{ e.user }}	{{ e.affiliation }}	{{ e.remarks }}
{% endfor %}
