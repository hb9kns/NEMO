# {{ eventtype }} totals for {% for p in projects %}{{ p.name }} {% endfor %}
# from {{ start }} to {{ end }} = {{ days }} days
Tool	Usage/min	Avg.Monthly/h	Reservation/min
{% for e in totals %}{{ e.ref }}	{{ e.desc }}	{{ e.usage }}	{{ e.monthly }}	{{ e.reservation }}
{% endfor %}
