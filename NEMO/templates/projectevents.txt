# {{ eventtype }} events for {{ projects }}
# from {{ start }} to {{ end }}
Start (ISO)	End (ISO)	Hours	Tool(s)	Project	User	Affiliation{% if eventtype == "reservation" %}	Title{% endif %}
{% for e in events %}{{ e.start|date:"y-m-d,H:i" }}	{{ e.end|date:"y-m-d,H:i" }}	{{ e.hours|floatformat:2 }}	{{ e.tooldesc }}	{{ e.projectdesc }}	{{ e.user }}	{{ e.affiliation }}{% if eventtype == "reservation" %}	{{ e.remarks }}{% endif %}
{% endfor %}
