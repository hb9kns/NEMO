# {{ eventtype }} events for {% for p in pjts %}{{ p.name }} {% endfor %}
# from {{ start }} to {{ end }}
Start (ISO)	End (ISO)	Minutes	Tool	Ref	Project	User	Affiliation{% if eventtype == "reservation" %}	Title{% endif %}
{% for e in events %}{{ e.start|date:"y-m-d,H:i" }}	{{ e.end|date:"y-m-d,H:i" }}	{{ e.minutes }}	{{ e.tooldesc }}	{{ e.toolref }}	{{ e.projectdesc }}	{{ e.user }}	{{ e.affiliation }}{% if eventtype == "reservation" %}	{{ e.remarks }}{% endif %}
{% endfor %}
